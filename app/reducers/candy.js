
const GET_CANDY = 'GET_CANDY'

const gotCandy = () => ({
    type: GET_CANDY
})

const getCandy = ()

const initialState = []

const candyReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}